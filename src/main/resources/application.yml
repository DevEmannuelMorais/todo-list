spring:
  datasource:
    url: jdbc:postgresql://localhost:2222/todolist?useTimezone=true&serverTimezone=UTC&useLegacyDate
    username: postgres
    password: 134546
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

